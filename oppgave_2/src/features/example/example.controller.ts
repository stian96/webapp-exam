// Retrieves query and validates.
// Imports service and NextApiRequest and NextApiResponse. Checks to see if all fields are present and validated. Returns HTTP codes. Returns 400 if some fields are missing. 
// Calls a service function. If false then returns 500, if true then returns 200.